config:
  warningsAsErrors: false

console-reports:
  active: true

output-reports:
  active: true

exceptions:
  SwallowedException:
    active: false

style:
  ForbiddenComment:
    active: false
  ThrowsCount:
    max: 4
